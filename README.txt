#3110 Program Assignment 1
Author: Jonathan Sanchez
EUID: js1612

In order to compile PartA.cpp use: g++ main.cpp PartA.cpp
Once compiled, execute PartA.cpp with: ./a.out

In order to compile PartB.cpp use: g++ main.cpp PartB.cpp
Once compiled, execute PartB.cpp with: ./a.out

This program will be reading in a file named "data1.txt" which is assumed to be in the current repository. Once each program is executed the output will be saved into a new file named "out1.txt" each line will be sorted and numbered. 

This program will read in up to 500 numbers delimited by a space per line, and sort them with merge and insertion sort based on which Part of the program is being ran.
